# Creatix — frontend client

This is a Vite + React starter for the Creatix UI. It includes Tailwind CSS and core dependencies used for the UI implementation.

Run locally:

```
cd creatix-client
npm install
npm run dev
```
